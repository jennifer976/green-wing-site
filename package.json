{
  "name": "greenwing-site",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "cms": "npx decap-server",
    "preview": "npm run build && npx --yes serve _site -l 3000"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
